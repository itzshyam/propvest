# Core
pydantic>=2.0,<3.0

# ABS Ingestor (Phase 1)
pandas>=2.0,<3.0
openpyxl>=3.1,<4.0   # Excel reading for ABS datacube files
requests>=2.31,<3.0

# Config
pyyaml>=6.0,<7.0

# Database
supabase>=2.0,<3.0

# Scraping
curl-cffi>=0.6,<1.0  # Domain + SQM — Chrome TLS impersonation (Akamai bypass)
# scrapegraphai      # Infrastructure pipeline parsing (activate in Phase 1 later)

# Backend (Phase 2)
# fastapi
# uvicorn[standard]
